'use client';

import Link from 'next/link';
import { FormEvent, useEffect, useMemo, useState } from 'react';
import { useAuth } from '@/contexts/AuthContext';
import LoginForm from '@/components/LoginForm';
import Logo from '@/components/Logo';
import { useTeamTasks } from '@/hooks/useTeamTasks';
import { TeamMember, TeamTask, TeamTaskInput, TeamTaskPriority, TeamTaskStatus } from '@/types/team-task';

const statuses: TeamTaskStatus[] = ['To Do', 'In Progress', 'Completed', 'On Hold'];
const priorities: TeamTaskPriority[] = ['Low', 'Medium', 'High', 'Urgent'];
const emptyForm: TeamTaskInput = { title: '', description: '', assignedTo: '', priority: 'Medium', status: 'To Do', dueDate: '', notes: '' };
const fieldClass = 'w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-900 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100';
const statusClass: Record<TeamTaskStatus, string> = { 'To Do': 'bg-slate-100 text-slate-700', 'In Progress': 'bg-blue-100 text-blue-700', Completed: 'bg-emerald-100 text-emerald-700', 'On Hold': 'bg-amber-100 text-amber-700' };
const priorityClass: Record<TeamTaskPriority, string> = { Low: 'bg-sky-50 text-sky-700', Medium: 'bg-amber-50 text-amber-700', High: 'bg-orange-50 text-orange-700', Urgent: 'bg-red-50 text-red-700' };

function toForm(task: TeamTask): TeamTaskInput {
  return { title: task.title, description: task.description, assignedTo: task.assignedTo.id, priority: task.priority, status: task.status, dueDate: task.dueDate?.slice(0, 10) || '', notes: task.notes };
}

function formatDate(value: string | null, short = false) {
  if (!value) return '—';
  return new Intl.DateTimeFormat('en-GB', short ? { day: 'numeric', month: 'short' } : { day: 'numeric', month: 'short', year: 'numeric' }).format(new Date(value));
}

function isOverdue(task: TeamTask) {
  if (!task.dueDate || task.status === 'Completed') return false;
  const due = new Date(task.dueDate); due.setHours(23, 59, 59, 999);
  return due < new Date();
}

function TaskForm({ initial, members, busy, onCancel, onSubmit }: { initial: TeamTaskInput; members: TeamMember[]; busy: boolean; onCancel: () => void; onSubmit: (value: TeamTaskInput) => void }) {
  const [form, setForm] = useState(initial);
  const set = <K extends keyof TeamTaskInput>(key: K, value: TeamTaskInput[K]) => setForm(v => ({ ...v, [key]: value }));
  const submit = (event: FormEvent) => { event.preventDefault(); if (form.title.trim() && form.assignedTo) onSubmit({ ...form, title: form.title.trim(), dueDate: form.dueDate || null }); };
  return <form onSubmit={submit} className="space-y-4">
    <div><label className="mb-1.5 block text-sm font-medium text-slate-700">Task title <span className="text-red-500">*</span></label><input autoFocus required maxLength={200} value={form.title} onChange={e => set('title', e.target.value)} className={fieldClass} placeholder="What needs to be done?" /></div>
    <div><label className="mb-1.5 block text-sm font-medium text-slate-700">Description</label><textarea rows={3} value={form.description} onChange={e => set('description', e.target.value)} className={fieldClass} placeholder="Add context or acceptance criteria…" /></div>
    <div className="grid gap-4 sm:grid-cols-2">
      <div><label className="mb-1.5 block text-sm font-medium text-slate-700">Assign to <span className="text-red-500">*</span></label><select required value={form.assignedTo} onChange={e => set('assignedTo', e.target.value)} className={fieldClass}><option value="">Select team member</option>{members.map(m => <option key={m.id} value={m.id}>{m.username}</option>)}</select></div>
      <div><label className="mb-1.5 block text-sm font-medium text-slate-700">Due date</label><input type="date" value={form.dueDate || ''} onChange={e => set('dueDate', e.target.value)} className={fieldClass} /></div>
      <div><label className="mb-1.5 block text-sm font-medium text-slate-700">Priority</label><select value={form.priority} onChange={e => set('priority', e.target.value as TeamTaskPriority)} className={fieldClass}>{priorities.map(p => <option key={p}>{p}</option>)}</select></div>
      <div><label className="mb-1.5 block text-sm font-medium text-slate-700">Status</label><select value={form.status} onChange={e => set('status', e.target.value as TeamTaskStatus)} className={fieldClass}>{statuses.map(s => <option key={s}>{s}</option>)}</select></div>
    </div>
    <div><label className="mb-1.5 block text-sm font-medium text-slate-700">Notes</label><textarea rows={2} value={form.notes} onChange={e => set('notes', e.target.value)} className={fieldClass} placeholder="Optional internal notes…" /></div>
    <div className="flex justify-end gap-2 border-t border-slate-100 pt-4"><button type="button" onClick={onCancel} className="rounded-lg border border-slate-200 px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50">Cancel</button><button disabled={busy || !form.title.trim() || !form.assignedTo} className="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800 disabled:opacity-50">{busy ? 'Saving…' : initial.title ? 'Save changes' : 'Create task'}</button></div>
  </form>;
}

export default function TeamTasksPage() {
  const { user, loading: authLoading, logout } = useAuth();
  const { tasks, members, loading, error, setError, fetchTasks, createTask, updateTask, deleteTask } = useTeamTasks();
  const [busy, setBusy] = useState(false); const [notice, setNotice] = useState('');
  const [modal, setModal] = useState<'add' | 'edit' | 'view' | 'delete' | null>(null); const [selected, setSelected] = useState<TeamTask | null>(null);
  const [scope, setScope] = useState<'all' | 'mine'>('all'); const [search, setSearch] = useState(''); const [status, setStatus] = useState('All'); const [priority, setPriority] = useState('All'); const [assignee, setAssignee] = useState('All'); const [due, setDue] = useState('All');

  useEffect(() => { if (user) fetchTasks(); }, [user, fetchTasks]);
  const toast = (message: string) => { setNotice(message); window.setTimeout(() => setNotice(''), 2800); };
  const save = async (value: TeamTaskInput) => { setBusy(true); const editing = modal === 'edit' && selected; const result = editing ? await updateTask(selected.id, value) : await createTask(value); setBusy(false); if (result) { setModal(null); setSelected(null); toast(editing ? 'Task updated' : 'Task created'); } };
  const changeStatus = async (task: TeamTask, next: TeamTaskStatus) => { setBusy(true); const result = await updateTask(task.id, { status: next }); setBusy(false); if (result) toast(next === 'Completed' ? 'Task completed' : `Moved to ${next}`); };
  const remove = async () => { if (!selected) return; setBusy(true); const success = await deleteTask(selected.id); setBusy(false); if (success) { setModal(null); setSelected(null); toast('Task deleted'); } };

  const filtered = useMemo(() => tasks.filter(task => {
    if (scope === 'mine' && task.assignedTo.id !== user?.id) return false;
    if (search && !`${task.title} ${task.description}`.toLowerCase().includes(search.toLowerCase())) return false;
    if (status !== 'All' && task.status !== status) return false; if (priority !== 'All' && task.priority !== priority) return false;
    if (assignee !== 'All' && task.assignedTo.id !== assignee) return false;
    if (due !== 'All') { if (!task.dueDate) return false; const d = new Date(task.dueDate); const now = new Date(); const start = new Date(now.getFullYear(), now.getMonth(), now.getDate()); const week = new Date(start); week.setDate(start.getDate() + 7); if (due === 'Today' && d.toDateString() !== now.toDateString()) return false; if (due === 'This Week' && !(d >= start && d < week)) return false; if (due === 'Overdue' && !isOverdue(task)) return false; }
    return true;
  }), [tasks, scope, search, status, priority, assignee, due, user?.id]);
  const stats = useMemo(() => ({ total: tasks.length, todo: tasks.filter(t => t.status === 'To Do').length, progress: tasks.filter(t => t.status === 'In Progress').length, completed: tasks.filter(t => t.status === 'Completed').length, overdue: tasks.filter(isOverdue).length }), [tasks]);

  if (authLoading) return <div className="flex min-h-screen items-center justify-center bg-slate-50 text-slate-500">Loading…</div>;
  if (!user) return <LoginForm />;
  if (!user.permissions.canViewTasks) return <div className="flex min-h-screen items-center justify-center bg-slate-50 text-slate-700">You do not have permission to view team tasks.</div>;

  const open = (kind: typeof modal, task?: TeamTask) => { setSelected(task || null); setModal(kind); setError(''); };
  return <div className="min-h-screen bg-slate-50 text-slate-900">
    {notice && <div className="fixed right-4 top-4 z-[70] rounded-lg bg-slate-900 px-4 py-3 text-sm font-medium text-white shadow-lg">{notice}</div>}
    <div className="flex min-h-screen">
      <aside className="sticky top-0 hidden h-screen w-64 flex-col border-r border-gray-200 bg-gray-50 md:flex"><div className="border-b border-gray-200 p-6"><Logo /></div><nav className="space-y-1 p-3"><Link href="/" className="flex items-center gap-2.5 rounded-lg px-3 py-2 text-sm font-medium text-gray-600 hover:bg-gray-100">← <span>Project workspace</span></Link><div className="flex items-center gap-2.5 rounded-lg bg-blue-50 px-3 py-2 text-sm font-medium text-blue-600"><span>✓</span><span>Team Tasks</span><span className="ml-auto rounded-full bg-blue-100 px-2 py-0.5 text-xs">{tasks.length}</span></div>{user.role === 'admin' && <Link href="/invoices" className="flex items-center gap-2.5 rounded-lg px-3 py-2 text-sm font-medium text-gray-600 hover:bg-gray-100">▤ <span>Invoices</span></Link>}</nav><div className="mt-auto border-t border-gray-200 p-4"><div className="flex items-center gap-3"><div className="flex h-8 w-8 items-center justify-center rounded-full bg-gray-900 text-sm text-white">{user.username[0].toUpperCase()}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{user.username}</p><p className="text-xs capitalize text-gray-500">{user.role.replace('_', ' ')}</p></div><button onClick={logout} title="Sign out" className="rounded p-2 text-gray-500 hover:bg-gray-100">↪</button></div></div></aside>
      <main className="min-w-0 flex-1">
        <header className="border-b border-slate-200 bg-white px-4 py-5 sm:px-6 lg:px-8"><div className="mx-auto flex max-w-[1500px] flex-wrap items-center justify-between gap-4"><div><div className="mb-2 flex items-center gap-3 md:hidden"><Link href="/" className="text-sm text-slate-500">← Projects</Link></div><h1 className="text-2xl font-semibold tracking-tight sm:text-3xl">Team Tasks</h1><p className="mt-1 text-sm text-slate-500">Manage and track your team’s work in one place.</p></div><button disabled={!user.permissions.canEditTasks} onClick={() => open('add')} className="rounded-lg bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white hover:bg-slate-800 disabled:opacity-50">+ Add Task</button></div></header>
        <div className="mx-auto max-w-[1500px] space-y-5 p-4 sm:p-6 lg:p-8">
          {error && <div className="flex justify-between rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700"><span>{error}</span><button onClick={() => setError('')}>×</button></div>}
          <section className="grid grid-cols-2 gap-3 lg:grid-cols-5">{([['Total Tasks', stats.total, 'text-slate-900'], ['To Do', stats.todo, 'text-slate-700'], ['In Progress', stats.progress, 'text-blue-600'], ['Completed', stats.completed, 'text-emerald-600'], ['Overdue', stats.overdue, 'text-red-600']] as const).map(([label, value, color]) => <div key={label} className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs font-medium uppercase tracking-wide text-slate-500">{label}</p><p className={`mt-2 text-2xl font-semibold ${color}`}>{value}</p></div>)}</section>
          <section className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">
            <div className="border-b border-slate-200 p-4"><div className="mb-4 flex gap-1 rounded-lg bg-slate-100 p-1 sm:w-fit"><button onClick={() => setScope('all')} className={`rounded-md px-4 py-2 text-sm font-medium ${scope === 'all' ? 'bg-white text-slate-900 shadow-sm' : 'text-slate-500'}`}>All Tasks</button><button onClick={() => setScope('mine')} className={`rounded-md px-4 py-2 text-sm font-medium ${scope === 'mine' ? 'bg-white text-slate-900 shadow-sm' : 'text-slate-500'}`}>My Tasks</button></div><div className="grid gap-2 md:grid-cols-2 xl:grid-cols-5"><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search tasks…" className={`${fieldClass} xl:col-span-1`} /><select value={status} onChange={e => setStatus(e.target.value)} className={fieldClass}><option>All</option>{statuses.map(s => <option key={s}>{s}</option>)}</select><select value={priority} onChange={e => setPriority(e.target.value)} className={fieldClass}><option>All</option>{priorities.map(p => <option key={p}>{p}</option>)}</select><select value={assignee} onChange={e => setAssignee(e.target.value)} className={fieldClass}><option value="All">All team members</option>{members.map(m => <option key={m.id} value={m.id}>{m.username}</option>)}</select><select value={due} onChange={e => setDue(e.target.value)} className={fieldClass}>{['All', 'Today', 'This Week', 'Overdue'].map(d => <option key={d}>{d}</option>)}</select></div></div>
            {loading ? <div className="p-16 text-center text-sm text-slate-500">Loading team tasks…</div> : filtered.length === 0 ? <div className="p-16 text-center"><div className="mx-auto mb-3 flex h-12 w-12 items-center justify-center rounded-full bg-slate-100 text-xl">✓</div><h2 className="font-semibold">No tasks found</h2><p className="mt-1 text-sm text-slate-500">Try changing your filters or create a new task.</p></div> : <>
              <div className="hidden overflow-x-auto lg:block"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{['Task', 'Assigned To', 'Created By', 'Priority', 'Status', 'Due Date', 'Created', 'Actions'].map(h => <th key={h} className="whitespace-nowrap px-4 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(task => <tr key={task.id} className="hover:bg-slate-50"><td className="max-w-xs px-4 py-3"><button onClick={() => open('view', task)} className="block truncate text-left font-medium hover:text-blue-600">{task.title}</button><p className="mt-0.5 truncate text-xs text-slate-500">{task.description || 'No description'}</p></td><td className="px-4 py-3">{task.assignedTo.username}</td><td className="px-4 py-3 text-slate-500">{task.createdBy.username}</td><td className="px-4 py-3"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${priorityClass[task.priority]}`}>{task.priority}</span></td><td className="px-4 py-3"><select disabled={busy || !user.permissions.canEditTasks} value={task.status} onChange={e => changeStatus(task, e.target.value as TeamTaskStatus)} className={`rounded-full border-0 px-2.5 py-1 text-xs font-medium outline-none ${statusClass[task.status]}`}>{statuses.map(s => <option key={s}>{s}</option>)}</select></td><td className="whitespace-nowrap px-4 py-3"><span className={isOverdue(task) ? 'font-medium text-red-600' : ''}>{formatDate(task.dueDate, true)}</span>{isOverdue(task) && <span className="ml-2 rounded bg-red-50 px-1.5 py-0.5 text-[10px] font-semibold uppercase text-red-600">Overdue</span>}</td><td className="whitespace-nowrap px-4 py-3 text-slate-500">{formatDate(task.createdAt, true)}</td><td className="px-4 py-3"><div className="flex gap-1"><button onClick={() => open('view', task)} title="View" className="rounded px-2 py-1 text-slate-500 hover:bg-slate-100">View</button>{user.permissions.canEditTasks && <><button onClick={() => open('edit', task)} className="rounded px-2 py-1 text-slate-500 hover:bg-slate-100">Edit</button>{task.status !== 'Completed' && <button onClick={() => changeStatus(task, 'Completed')} title="Mark complete" className="rounded px-2 py-1 text-emerald-600 hover:bg-emerald-50">✓</button>}<button onClick={() => open('delete', task)} title="Delete" className="rounded px-2 py-1 text-red-500 hover:bg-red-50">•••</button></>}</div></td></tr>)}</tbody></table></div>
              <div className="divide-y divide-slate-100 lg:hidden">{filtered.map(task => <article key={task.id} className="p-4"><div className="flex items-start justify-between gap-3"><button onClick={() => open('view', task)} className="text-left font-semibold">{task.title}</button><span className={`rounded-full px-2 py-1 text-xs ${priorityClass[task.priority]}`}>{task.priority}</span></div><p className="mt-1 line-clamp-2 text-sm text-slate-500">{task.description || 'No description'}</p><div className="mt-3 flex flex-wrap items-center gap-2 text-xs"><select value={task.status} onChange={e => changeStatus(task, e.target.value as TeamTaskStatus)} className={`rounded-full border-0 px-2 py-1 ${statusClass[task.status]}`}>{statuses.map(s => <option key={s}>{s}</option>)}</select><span className="text-slate-500">{task.assignedTo.username}</span><span className={isOverdue(task) ? 'font-medium text-red-600' : 'text-slate-500'}>{formatDate(task.dueDate)}</span></div><div className="mt-3 flex gap-2"><button onClick={() => open('view', task)} className="text-sm text-slate-600">View</button>{user.permissions.canEditTasks && <><button onClick={() => open('edit', task)} className="text-sm text-blue-600">Edit</button><button onClick={() => open('delete', task)} className="text-sm text-red-600">Delete</button></>}</div></article>)}</div>
            </>}
          </section>
        </div>
      </main>
    </div>
    {modal && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/50 p-4" onMouseDown={e => { if (e.target === e.currentTarget && !busy) setModal(null); }}><div className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><h2 className="text-lg font-semibold">{modal === 'add' ? 'Add Task' : modal === 'edit' ? 'Edit Task' : modal === 'delete' ? 'Delete this task?' : 'Task Details'}</h2><button onClick={() => setModal(null)} className="rounded p-1 text-xl text-slate-400 hover:bg-slate-100">×</button></div><div className="p-5">{modal === 'add' && <TaskForm initial={{ ...emptyForm, assignedTo: user.id }} members={members} busy={busy} onCancel={() => setModal(null)} onSubmit={save} />}{modal === 'edit' && selected && <TaskForm initial={toForm(selected)} members={members} busy={busy} onCancel={() => setModal(null)} onSubmit={save} />}{modal === 'delete' && selected && <div><p className="text-sm text-slate-600">“{selected.title}” will be permanently deleted. This action cannot be undone.</p><div className="mt-6 flex justify-end gap-2"><button onClick={() => setModal(null)} className="rounded-lg border border-slate-200 px-4 py-2 text-sm">Cancel</button><button disabled={busy} onClick={remove} className="rounded-lg bg-red-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{busy ? 'Deleting…' : 'Delete'}</button></div></div>}{modal === 'view' && selected && <div><div className="flex flex-wrap gap-2"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${priorityClass[selected.priority]}`}>{selected.priority}</span><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${statusClass[selected.status]}`}>{selected.status}</span>{isOverdue(selected) && <span className="rounded-full bg-red-100 px-2.5 py-1 text-xs font-medium text-red-700">Overdue</span>}</div><p className="mt-5 whitespace-pre-wrap text-sm leading-6 text-slate-600">{selected.description || 'No description provided.'}</p><dl className="mt-6 grid gap-4 rounded-xl bg-slate-50 p-4 sm:grid-cols-2">{[['Assigned to', selected.assignedTo.username], ['Created by', selected.createdBy.username], ['Due date', formatDate(selected.dueDate)], ['Created', formatDate(selected.createdAt)], ['Last updated', formatDate(selected.updatedAt)], ['Completed', formatDate(selected.completedAt)]].map(([k, v]) => <div key={k}><dt className="text-xs uppercase tracking-wide text-slate-400">{k}</dt><dd className="mt-1 text-sm font-medium">{v}</dd></div>)}</dl>{selected.notes && <div className="mt-5"><h3 className="text-sm font-semibold">Notes</h3><p className="mt-2 whitespace-pre-wrap rounded-lg border border-slate-200 p-3 text-sm text-slate-600">{selected.notes}</p></div>}<div className="mt-6 flex justify-end gap-2"><button onClick={() => setModal(null)} className="rounded-lg border border-slate-200 px-4 py-2 text-sm">Close</button>{user.permissions.canEditTasks && <button onClick={() => setModal('edit')} className="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Edit task</button>}</div></div>}</div></div></div>}
  </div>;
}
